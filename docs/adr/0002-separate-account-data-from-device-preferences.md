# Separate account data from device preferences

Offline synchronization includes planner data, planner behavior, language, and the last-opened date, while visual layout and device capabilities remain local to each installation. This prevents one device from changing another device's theme, dimensions, fullscreen state, or notification choices, while preserving the user's explicit expectation that language and navigation position follow the account.
